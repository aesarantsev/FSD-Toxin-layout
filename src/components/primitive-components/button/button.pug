////
//Types buttons - fillButton, borerButton, labelButton, arrowButton
////
mixin button (text = "Click Me please", type = "fillButton",link="#")
  case type
    when "labelButton"
      a.btn.btn--label(href=link)=text
    when "borerButton"
      a.btn.btn--border(href=link)=text
    when "fillButton"
      a.btn.btn--fill(href=link)=text
    when "arrowButton"
      a.btn.btn--fill.bnt--label--arrow(href=link)=text
    
    